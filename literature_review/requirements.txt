# 文献综述流水线 - 依赖包列表
# Python 版本: 3.10, 3.11 均可

# PDF 解析（使用 MinerU REST API，无需 SDK）
# API 调用只需要 requests 库

# HTTP 请求
requests>=2.28.0

# 数据处理
python-dateutil>=2.8.0

# 可选：进度条显示
tqdm>=4.65.0

# Embedding 模型（相关度计算）
sentence-transformers>=2.0.0
scikit-learn>=1.0.0

# Python 版本要求：3.10 或 3.11

# ===== 安装说明 =====
# 直接安装所有依赖：
pip install requests python-dateutil tqdm

# ===== 配置说明 =====
# 使用 MinerU REST API 需要设置环境变量：
# export MINERU_API_TOKEN='your_api_token'
#
# 获取 API Token：访问 https://mineru.net 注册并获取
